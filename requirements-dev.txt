# Development dependencies for pi-camera-in-docker
# Install with: python -m pip install -r requirements-dev.txt

# Include production requirements
-r requirements.txt

# Optional telemetry integration is included so tests cover the configured Sentry path.
-r requirements-sentry.txt

# YAML is used only by repository workflow-policy tests, not by the runtime app.
PyYAML>=6.0.3

# Packaging and build tools
setuptools==84.0.0

# Testing framework
pytest==9.1.1
pytest-cov==7.1.0
pytest-mock==3.15.1

# Code quality tools
ruff==0.16.8

# Code coverage
coverage==7.16.1

# Type checking
mypy==2.3.1
types-Flask==1.1.6

# Pre-commit hooks
pre-commit==4.6.2

# Local security scanning
bandit==1.9.4
