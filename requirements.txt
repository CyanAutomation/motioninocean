# Core dependencies for pi-camera-in-docker
# These should be installed via pip in the Docker container

# Web framework
Flask==3.1.2

# CORS support for Flask
flask-cors==6.0.2

# Lightweight image generation for mock camera fallback
Pillow==11.3.0

# Computer vision - minimal headless version (saves ~200MB vs python3-opencv)
# NOTE: This is now conditionally installed via Dockerfile build arg INCLUDE_OPENCV
# Default build excludes this dependency to save ~40MB
# To include edge detection support, build with: --build-arg INCLUDE_OPENCV=true
opencv-python-headless==4.13.0.90

# Camera support for Raspberry Pi
# Note: picamera2 is now installed via apt from Raspberry Pi repository
# (python3-picamera2) along with system packages (python3-libcamera, libcamera-dev)
# The pip version is unreliable and not the recommended installation method for Bookworm

# Development and testing dependencies
# These are listed here for reference but installed separately in CI/dev environments
# pytest==8.3.4
# pytest-cov==6.0.0
# coverage==7.6.9
# ruff==0.8.4
# pre-commit==4.0.1
# mypy==1.13.0
# types-Flask==1.1.6
