# Core dependencies for pi-camera-in-docker
# These should be installed via pip in the Docker container

# Web framework
Flask==3.1.3

# CORS support for Flask
flask-cors==6.0.2

# Rate limiting for Flask APIs
Flask-Limiter==3.11.0

# Error tracking and APM
sentry-sdk[flask]==2.19.0

# Lightweight image generation for mock camera fallback
# Included in all builds so runtime can switch to mock mode via MOCK_CAMERA=true when needed.
Pillow>=12.1.1

# NumPy for array processing
numpy==2.2.3

# Camera support for Raspberry Pi
# Note: picamera2 is installed separately via conditional pip install in the Dockerfile
# builder stage for arm64 only (see Dockerfile line ~52).
# It is NOT in requirements.txt to avoid unnecessary build of C dependencies on amd64.
# C extensions (libcamera, python3-libcamera) are installed via apt in the final stage.

# Development and testing dependencies
# These are listed here for reference but installed separately in CI/dev environments
# pytest==8.3.4
# pytest-cov==6.0.0
# coverage==7.6.9
# ruff==0.8.4
# pre-commit==4.0.1
# mypy==1.13.0
# types-Flask==1.1.6
