# Security constraints for the fully resolved production dependency graph.
#
# flask-compress pulls msgpack into the runtime environment transitively. Keep the
# transitive package above the patched floor without making it a direct application
# dependency; the application itself does not import msgpack.
msgpack>=1.2.1
