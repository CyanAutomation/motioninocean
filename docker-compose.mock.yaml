# Override for MOCK_CAMERA mode - removes device dependencies
# ⚠️  DEPRECATED: Use containers/motioniocean-webcam/docker-compose.mock.yml instead
# This file is kept for backward compatibility but will be removed in a future release.
# See MIGRATION.md for upgrade guidance.
#
# Redefine the service without camera device anchor for mock mode
version: '3.8'

services:
  motion-in-ocean:
    # Remove all device mappings and group additions for mock camera mode
    devices: []
    group_add: []
    device_cgroup_rules: []

